[project]
name = "gitsage"
version = "0.1.0"
description = "Local LLM powered Git commit generator"
requires-python = ">=3.10"
dependencies = [
    "requests",
    "pyperclip",
]

[project.scripts]
gitsage = "gitsage.cli:main"